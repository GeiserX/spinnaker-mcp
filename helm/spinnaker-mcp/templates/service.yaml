{{- if eq .Values.transport "http" }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "spinnaker-mcp.fullname" . }}
  labels:
    {{- include "spinnaker-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spinnaker-mcp.selectorLabels" . | nindent 4 }}
{{- end }}
